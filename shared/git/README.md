# Git

Global [Git](https://git-scm.com) configuration.

## Highlights

- Sets `user.email` / `user.name`
- `pull.rebase false`

## Setup

`setup.sh` applies the settings directly via `git config --global` (no config file is symlinked).
